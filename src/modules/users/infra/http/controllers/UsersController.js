import { container } from "tsyringe";

//aqui ficam os metodos para edicao e atualizacao no banco
class UsersController {
    // async create(req, res){
    //     const {user} = req.body;

    //     const createUser = container.resolve(CreateUserService);

    // }
}